#!/bin/bash
# Optimized start script for Django deployment
echo "Starting Django application with optimizations..."

# Start the Django server with startup optimizations
# Using --nothreading and --insecure for faster startup in deployment
exec python manage.py runserver 0.0.0.0:8000 --nothreading --insecure